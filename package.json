{
  "name": "<$name>",
  "version": "0.1.0",
  "description": "<$description>",
  "main": "index.js",
  "scripts": {
    "build": "pnpm run clean && tsc",
    "clean": "rimarf dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  }
}
